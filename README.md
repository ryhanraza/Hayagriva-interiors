# Hayagriva Interiors — Next.js + Tailwind + Framer Motion

Starter scaffold for a premium interior design website using Next.js (App Router), Tailwind CSS and Framer Motion.

Run locally:

```powershell
npm install
npm run dev
```

Pages: `/`, `/about`, `/services`, `/portfolio`, `/projects`, `/contact`
